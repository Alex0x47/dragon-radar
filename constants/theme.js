const COLORS = {
    primary_btn: "#ffffff",
    secondary_btn: "#ffffff",
    primary_txt: "#ffffff",
    secondary_txt: "#ffffff",
};

const BUTTONS = {
    play: {
        bg_color: COLORS.primary_btn,
        txt_color: COLORS.primary_txt
    },
    settings: {
        bg_color: COLORS.primary_btn,
        txt_color: COLORS.primary_txt
    }
};

const RADAR = {

};

const BALLS = {

};

const SIZES = {
    
};

const FONTS = {
    
}